/**
 * Constants related to tasks.
 */
export const constants = {
    /**
     * Predefined task statuses.
     * @type {string[]}
     */
    task_status: ["TO_DO", "TOP_PRIORITY", "LOW_PRIORITY", "HOLD", "CANCELLED"]
};
